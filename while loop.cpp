 //Using While loop for same work.....    :)
  int a;
  a=0;
  while(s[a]!=NULL)
  {
    if(s[i]=='a' || s[i]=='e' || s[i]=='i' || s[i]=='o' || s[i]=='u')
  	{
  	switch(s[a])
    {
      case'i':
      case'a':
      case'e':
      case'o':
      case'u':
      case'A':
      case'E':
      case'I':
      case'O':
      case'U':
      v++;
    }
    a++;
   }
   c++;  
  }
